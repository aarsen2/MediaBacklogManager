export interface BaseForm {
    title: string   
    mediaType: MediaType
    releaseDate: string
    description: string
    genres: string[]
    recommenders: string[]
    status: string
    prioritized: string
    userRating: number
    notes: string
}