image: node:20

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

before_script:
  - cd frontend/media-backlog-frontend
  - npm install

build:
  stage: build
  script:
    - npm run build -- --configuration production
  artifacts:
    paths:
      - dist/

deploy:
  stage: deploy
  script:
    - npm install -g @azure/static-web-apps-cli
    - swa deploy ./dist/media-backlog-frontend \
        --deployment-token $AZURE_STATIC_WEB_APPS_API_TOKEN \
        --env production
  only:
    - main